# Ponytail Prompt

Read `AGENTS.md` first.

Use the lazily correct solution:

- ask whether this needs to exist
- reuse existing helpers, stdlib, and native features first
- avoid new dependencies and broad abstractions
- keep the diff as small as possible

If the task is non-trivial, prefer the smallest working change and call out any deliberate simplification with a `ponytail:` comment.

Fill in:

- Goal:
- Files:
- Constraints:
- Tests:
