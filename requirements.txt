# Runtime dependencies — must stay in sync with pyproject.toml [project.dependencies].
# For development, use: pip install -e ".[dev]"
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.0,<5.0.0
lxml>=4.9.0,<6.0.0
